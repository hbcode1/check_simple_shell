# Values

R=$RANDOM
TIME=3
OUTPUT="output_$R"

# Commands pathnames
WHICH="/usr/bin/which"
GCC=`$WHICH gcc`
DIFF=`$WHICH diff`
CAT=`$WHICH cat`
SLP=`$WHICH sleep`
ECHO=`$WHICH echo`
WC=`$WHICH wc -c`
RM=`$WHICH rm`
TOUCH=`$WHICH touch`
CHMOD=`$WHICH chmod`
PIDOF=`$WHICH pidof`
KILLALL=`$WHICH killall`
KILL=`$WHICH kill`
CLR=`$WHICH clear`
